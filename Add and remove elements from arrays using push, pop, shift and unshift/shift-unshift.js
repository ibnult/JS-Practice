const friends = ['Alice', 'Bob', 'Charlie'];
console.log(friends);
// Shift elements from the beginning of the array
friends.shift();
console.log(friends);
// Unshift elements to the beginning of the array
friends.unshift('Dave', 'Eve');
console.log(friends);